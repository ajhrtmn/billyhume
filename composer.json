{
    "name": "own-ur-shit/dev-tooling",
    "description": "Dev-only tooling for running this repo's pure-logic PHPUnit suites (wp-content/plugins/*/tests/). Not a runtime dependency of any plugin — WordPress itself is loaded from wp-load.php, never Composer's autoloader.",
    "require-dev": {
        "phpunit/phpunit": "^9"
    },
    "config": {
        "platform": {
            "php": "8.1"
        }
    }
}
